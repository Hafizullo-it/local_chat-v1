# 🚀 Modern Chat (Offline Edition)

Cовременный и быстрый чат для локальных сетей. Работает полностью без интернета, не использует внешние CDNs или API. Идеально подходит для офисов, частных сетей или использования в местах без доступа к сети.

## 🌟 Особенности
- **Полная автономность**: Все библиотеки (Tailwind, FontAwesome) включены в проект.
- **WebSocket Реального времени**: Мгновенная доставка сообщений.
- **Приватность**: Личные сообщения и общие комнаты.
- **Админ-панель**: Управление пользователями, бан/удаление, редактирование профилей.
- **Файловый менеджер**: Отправка любых файлов и изображений.
- **Защита**: Встроенная блокировка по IP и XSS фильтрация.

## 💻 Как запустить на другом компьютере

Этот проект максимально портативен. Для переноса на другой ПК:

1. **Скопируйте папку** `modern_chat` на нужный компьютер (через флешку или сеть).
2. **Убедитесь, что установлен Node.js** (скачайте с nodejs.org, если его нет).
3. **Запустите сервер**:
   ```powershell
   cd modern_chat
   npm start
   ```
4. **Узнайте свой IP адрес** (для доступа других пользователей):
   - Откройте терминал и введите `ipconfig` (Windows) или `ifconfig` (Linux/Mac).
   - Найдите строку `IPv4-адрес` (например, `192.168.1.5`).
5. **Подключитесь**:
   - На вашем ПК: `http://localhost:3000`
   - С других ПК в этой же сети: `http://192.168.1.5:3000` (замените на ваш IP).

## 👤 Доступ по умолчанию
- **Админ**: `admin` / `adminpass`
- *Рекомендуется сменить пароль в настройках профиля (иконка шестеренки).*

## 🛠 Требования
- **Node.js 16+** (единственная зависимость)
- **Оперативная память**: от 500 МБ
- **Браузер**: Любой современный (Chrome, Firefox, Edge)

---
*Проект подготовлен для работы в закрытых сетях.*