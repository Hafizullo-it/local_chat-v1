# 🚀 Развертывание Modern Chat (Оффлайн режим)

## 📋 Системные требования

- **Node.js** версии 16.x или выше
- **Операционная система**: Windows, Linux, macOS
- **Свободное место**: минимум 200 MB
- **Память**: минимум 512 MB RAM

## 📦 Что входит в проект

Проект содержит все необходимые файлы для работы в оффлайн режиме:

### Обязательные файлы:
- `server.js` - основной сервер
- `package.json` & `package-lock.json` - конфигурация зависимостей
- `node_modules/` - установленные библиотеки Node.js
- `public/` - клиентская часть:
  - `chat.html` - основной интерфейс чата
  - `admin.html` - панель администратора
  - `script.js` - клиентская логика
  - `libs/` - локальные стили и библиотеки
  - `img/` - изображения и аватары
  - `uploads/` - загруженные файлы пользователей

### Автоматически создаваемые файлы:
- `data/users.db` - база данных пользователей
- `data/messages.db` - база данных сообщений

## 🔧 Установка и запуск

### Шаг 1: Установка Node.js

Если Node.js не установлен:

#### Windows:
1. Скачайте установщик с [nodejs.org](https://nodejs.org/)
2. Запустите установщик
3. Перезагрузите командную строку/PowerShell

#### Linux/Ubuntu:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### macOS:
```bash
brew install node
```

Проверка установки:
```bash
node --version
npm --version
```

### Шаг 2: Копирование проекта

1. Скопируйте всю папку `modern_chat` на целевой компьютер
2. Убедитесь что все файлы скопировались (особенно `node_modules/`)

### Шаг 3: Запуск сервера

#### Вариант 1: Быстрый запуск (рекомендуется)
```bash
cd modern_chat
npm start
```

#### Вариант 2: Ручной запуск
```bash
cd modern_chat
node server.js
```

### Шаг 4: Доступ к чату

После запуска откройте браузер и перейдите:
- **Основной чат**: http://localhost:3000
- **Админ-панель**: доступна через интерфейс чата (шестеренка → админ-панель)

## 👤 Первый запуск и админ-доступ

При первом запуске автоматически создается администратор:
- **Логин**: `admin`
- **Пароль**: `adminpass`

⚠️ **Важно**: Сразу после первого входа измените пароль администратора!

### Админ-панель

Админ-панель доступна через шестеренку в основном чате. В ней доступны функции:

- **Просмотр всех пользователей** с статусами (онлайн/оффлайн/забанен)
- **Бан/разбан пользователей** (сообщения остаются, аккаунт блокируется)
- **Полное удаление пользователей** (⚠️ удаляет все сообщения и аккаунт навсегда)
- **Изменение данных пользователей** (имя, роль, аватар)

## 🔧 Возможные проблемы и решения

### ❌ Ошибка: "node is not recognized as an internal or external command"

**Решение**:
1. Переустановите Node.js
2. Перезагрузите командную строку
3. Проверьте переменную PATH

### ❌ Ошибка: "Port 3000 is already in use"

**Решение**:
```bash
# Найти процесс использующий порт 3000
netstat -ano | findstr :3000

# Убить процесс (замените PID)
taskkill /PID <PID> /F

# Или запустить на другом порту (измените в server.js)
```

### ❌ Ошибка: "Cannot find module" или отсутствующие модули

**Решение**:
```bash
# Удалить и переустановить node_modules
rmdir /s /q node_modules
npm install
```

### ❌ Чат не загружается / пустая страница

**Решение**:
1. Проверьте что сервер запущен
2. Проверьте порт (должен быть 3000)
3. Очистите кэш браузера (Ctrl+F5)
4. Проверьте файлы в папке `public/libs/`

### ❌ Не отображаются иконки (изменить, удалить, настройки)

**Решение**:
1. Проверьте что файл `public/libs/fontawesome.min.css` существует
2. Проверьте что папка `public/libs/webfonts/` содержит шрифты
3. Очистите кэш браузера (Ctrl+F5)
4. Проверьте консоль браузера на ошибки загрузки шрифтов

### ❌ Админ-панель не работает

**Решение**:
1. Убедитесь что вошли под пользователем `admin`
2. Проверьте права доступа
3. Попробуйте перезапустить сервер

### ❌ Изображения/аватары не отображаются

**Решение**:
1. Проверьте папку `public/img/`
2. Проверьте права доступа к файлам
3. Убедитесь что файл `default-avatar.png` существует

## 🔄 Обновление проекта

При получении обновлений:

1. **Остановите сервер** (Ctrl+C)
2. **Замените файлы** новыми версиями
3. **Если обновились зависимости**:
   ```bash
   npm install
   ```
4. **Запустите сервер** снова

## 🛡️ Безопасность

### Важные настройки:

1. **Измените пароль администратора** после первого входа
2. **Не запускайте на публичных IP** без дополнительной настройки
3. **Регулярно обновляйте Node.js**
4. **Делайте бэкапы** папки `data/`

### Защита от внешнего доступа:

Если сервер должен быть доступен только локально, добавьте в `server.js`:

```javascript
// Добавить после const app = express();
app.set('trust proxy', 1);
app.use((req, res, next) => {
    const clientIP = req.ip || req.connection.remoteAddress;
    if (!clientIP.startsWith('127.') && !clientIP.startsWith('192.168.') && !clientIP.startsWith('10.')) {
        return res.status(403).send('Access denied');
    }
    next();
});
```

## 📁 Структура проекта

```
modern_chat/
├── server.js                 # Сервер Node.js
├── package.json             # Конфигурация проекта
├── package-lock.json        # Фиксация версий зависимостей
├── node_modules/            # Установленные библиотеки
├── data/                    # Базы данных (создается автоматически)
│   ├── users.db
│   └── messages.db
└── public/                  # Клиентская часть
    ├── chat.html            # Основной интерфейс
    ├── admin.html           # Панель администратора
    ├── script.js            # Клиентская логика
    ├── bootstrap.min.css    # Стили Bootstrap
    ├── css/
    │   └── style.css        # Дополнительные стили
    ├── libs/                # Локальные библиотеки
    │   ├── tailwind.min.css # Tailwind CSS (локально)
    │   ├── fontawesome.min.css # Font Awesome CSS (локально)
    │   └── webfonts/         # Шрифты Font Awesome
    ├── img/                 # Изображения и аватары
    └── uploads/             # Загруженные файлы
```

## 🚀 Быстрый чек-лист перед запуском

- ✅ Node.js установлен (версия 16+)
- ✅ Все файлы скопированы
- ✅ `node_modules/` присутствует (зависимости установлены)
- ✅ Папка `public/libs/` содержит стили и шрифты
- ✅ Порт 3000 свободен
- ✅ Браузер поддерживает современный JavaScript

## 🔍 Тестирование после запуска

После запуска сервера проверьте:

1. **Основной чат**: http://localhost:3000
2. **Комплексный тест оффлайн работы**: http://localhost:3000/offline-test.html
3. **Тест иконок**: http://localhost:3000/test-icons.html (для проверки Font Awesome)

### 🚨 Если что-то не работает:

#### Проблема: Иконки не отображаются
- Откройте http://localhost:3000/test-icons.html
- Очистите кэш браузера (Ctrl+F5)
- Проверьте консоль на ошибки загрузки шрифтов

#### Проблема: Страница не загружается
- Проверьте что сервер запущен: `netstat -ano | findstr :3000`
- Попробуйте другой порт в server.js
- Проверьте файлы в папке `public/`

#### Проблема: Ошибки 404 на ресурсы
- Убедитесь что папка `public/libs/` существует
- Проверьте что файлы `tailwind.min.css` и `fontawesome.min.css` присутствуют

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи сервера в консоли
2. Очистите кэш браузера
3. Перезапустите сервер
4. Проверьте права доступа к файлам

При возникновении ошибок сохраните полный текст ошибки для диагностики.